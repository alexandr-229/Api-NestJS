English:

    To run the application:

            git clone https://github.com/alexandr-229/Api-NestJS.git

            add a file ".env"

            write into it:

                LINK_TO_DB=mongodb://<Your link>

                JWT_SECRET=<There's a little fantasy>

                TELEGRAM_TOKEN=<Next, you need to generate a telegram bot>
                
                CHAT_ID=<Your ID in telegram>

            docker build -t <Еitle> ./Dockerfile

        or:

            git clone https://github.com/alexandr-229/Api-NestJS.git

            npm i --force

            add a file ".env"

            write into it:

                LINK_TO_DB=mongodb://<Your link>

                JWT_SECRET=<There's a little fantasy>

                TELEGRAM_TOKEN=<Next, you need to generate a telegram bot>
                
                CHAT_ID=<Your ID in telegram>


Русский:

    Что-бы запустить приложение:

            git clone https://github.com/alexandr-229/Api-NestJS.git

            добавить файл ".env"

            в него записать:

                LINK_TO_DB=mongodb://<Далее Ваша сыылка>

                JWT_SECRET=<Тут немного фантазии>

                TELEGRAM_TOKEN=<Дальше нужно сгенерировать telegram-бота>
                
                CHAT_ID=<Ваш ID в telegram>

            docker build -t <Название> ./Dockerfile

        или:

            git clone https://github.com/alexandr-229/Api-NestJS.git

            npm i --force

            добавить файл ".env"

            в него записать:

                LINK_TO_DB=mongodb://<Далее Ваша сыылка>

                JWT_SECRET=<Тут немного фантазии>

                TELEGRAM_TOKEN=<Дальше нужно сгенерировать telegram-бота>
                
                CHAT_ID=<Ваш ID в telegram>